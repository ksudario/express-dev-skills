<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Express Dev Skills</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h1>👩🏽‍💻 Here Are My Developer Skills</h1>
    <h3>🏡 <a href="/">Back Home</a></h3>
    <ul>
        <% skills.forEach(function(skill, idx){ %>
            <li>
                💻 <a href="/skills/<%= idx %>"><%=skill.text %></a>
                -
                <%= skill.acquired ? 'Yes' : 'No' %>

                <form action="/skills/<%= idx %>?_method=PUT"
                    class="update-form" method="POST">
                    <input type ="text" placeholder="Edit Skill!" name = "newText" value="<%= skill.text %>">
                    <button type = "submit"> Update</button>
                </form>

                <form action="/skills/<%= idx %>?_method=DELETE"
                        class="delete-form" method="POST">
                        <button type="submit">Delete</button>
                </form>
           
            </li>
        <% }); %>
    </ul>
    <h3>🦾<a href="/skills/new"> Add A New Skill</a></h3>
</body>
</html>